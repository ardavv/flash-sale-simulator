{
  "name": "gateway",
  "version": "1.0.0",
  "description": "Order Gateway — HTTP server that forwards orders to Inventory via TCP",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "express": "^4.18.2",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "fast-check": "^3.14.0"
  }
}
