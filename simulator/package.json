{
  "name": "simulator",
  "version": "1.0.0",
  "description": "Client Simulator — spawns worker threads to generate concurrent HTTP load",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "fast-check": "^3.14.0"
  }
}
