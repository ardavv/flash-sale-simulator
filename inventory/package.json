{
  "name": "inventory",
  "version": "1.0.0",
  "description": "Inventory Coordinator — TCP server with mutex-protected stock management",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest --runInBand"
  },
  "dependencies": {},
  "devDependencies": {
    "jest": "^29.7.0",
    "fast-check": "^3.14.0"
  }
}
